export type Project = {
  title: string;
  description: string;
  technologies: string[];
  url: string;
};

export type Contact = {
  title: string;
  email: string;
  phone: string;
  address: string;
};

export type About = {
  title: string;
  body: string;
};
