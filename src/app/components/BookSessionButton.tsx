const BookSessionButton = () => {
  return (
    <div className="fixed bottom-4 right-4 z-50">
      <button className="bg-orange-600 hover:bg-orange-700 text-white font-bold py-2 px-4 rounded-full shadow-lg transition duration-300">
        Book a session
      </button>
    </div>
  );
};

export default BookSessionButton;
